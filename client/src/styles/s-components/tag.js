import styled from 'styled-components'
import { CustomInput, FlexBox } from '../s-pages/write'

export const TagInput = styled(CustomInput)`
  width: 100%;
`

export const TagListWrapper = styled(FlexBox)`
  margin-top: 10px;
  min-height: 18px;
  justify-content: flex-start;
`
